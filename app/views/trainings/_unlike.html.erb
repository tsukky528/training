<%= form_for(current_user.relationships.find_by(like_movie_id: @training),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "お気に入りから削除", class: "btn btn-mini" %>
<% end %>